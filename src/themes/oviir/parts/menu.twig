<nav class="navbar navbar-b navbar-trans navbar-expand-md fixed-top" id="mainNav">
  <div class="container">
    <a class="navbar-brand js-scroll" href="{{ base_url }}">Oviir.eu</a>
    <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarDefault"
      aria-controls="navbarDefault" aria-expanded="false" aria-label="Toggle navigation">
      <span></span>
      <span></span>
      <span></span>
    </button>
    <div class="navbar-collapse collapse justify-content-end" id="navbarDefault">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link {{ current_page.id starts with 'uudised' ? 'active' : '' }}" href="{{ pages['uudised'].url }}">Uudised</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ current_page.id starts with 'pildid' ? 'active' : '' }}" href="{{ pages['pildid'].url }}">Pildid</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle {{ current_page.meta.category == 'suguvõsa' ? 'active' : '' }}"
            href="#"
            id="navbarDropdown"
            role="button"
            data-toggle="dropdown"
            aria-haspopup="true"
            aria-expanded="false">
            Suguvõsa
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item {{ current_page.id == 'ajalugu' ? 'active' : '' }}" href="{{ pages['ajalugu'].url }}">Ajalugu</a>
            <a class="dropdown-item {{ current_page.id starts with 'kokkutulekud' ? 'active' : '' }}" href="{{ pages['kokkutulekud'].url }}">Kokkutulekud</a>
            {% if user %}
            <a class="dropdown-item {{ current_page.id == 'oviirid' ? 'active' : '' }}" href="{{ pages['oviirid'].url }}">{{ pages['oviirid'].meta.title }}</a>
            <a class="dropdown-item {{ current_page.id == 'sugupuu' ? 'active' : '' }}" href="{{ pages['sugupuu'].url }}">Sugupuu kaart</a>
            <a class="dropdown-item {{ current_page.id starts with 'kontaktid' ? 'active' : '' }}" href="{{ pages['kontaktid'].url }}">Sugulaste kontaktid</a>
            <a class="dropdown-item {{ current_page.id starts with 'varasalv' ? 'active' : '' }}" href="{{ pages['varasalv'].url }}">Varasalv</a>
            {% endif %}
            <div class="dropdown-divider"></div>
            <a class="dropdown-item {{ current_page.id == 'teave' ? 'active' : '' }}" href="{{ pages['teave'].url }}">Teave</a>
          </div>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link"
            href="#"
            id="loginDropdown"
            role="button"
            data-toggle="dropdown"
            aria-haspopup="true"
            aria-expanded="false"
            style="padding-top: 0.1rem; font-size: 1.5rem;">
            <i class="icon ion-ios-contact"></i>
          </a>
          <div class="dropdown-menu dropdown-menu-right" style="width:250px">

            {% include 'parts/login.twig' %}

            <div class="dropdown-divider"></div>
            <a class="dropdown-item text-muted small" href="{{ pages['teave'].url }}">{{ user ? 'Teata probleemist' : 'Raskusi logimisega?' }}</a>
          </div>

        </li>
      </ul>
    </div>
  </div>
</nav>
